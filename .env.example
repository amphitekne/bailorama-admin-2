# URL completa de la API. Se usa de dos formas:
#   - En dev: Vite extrae el origin y hace proxy de /api/* → evita CORS
#   - En prod: el cliente la usa directamente
VITE_BAILORAMA_API_URL=https://tu-subdominio.ngrok-free.dev/api
